export default async () => { const now=new Date(); const br=new Date(now.toLocaleString('en-US',{timeZone:'America/Sao_Paulo'})); const day=br.getDay(), mins=br.getHours()*60+br.getMinutes(); const open=(day===0||day>=2)&&mins>=1110&&mins<=1439; return Response.json({open,hours:'Ter–dom, 18:30–23:59',estimate:'45–60 min',pix_key:'47999821938'}); }; export const config={path:'/api/status'};