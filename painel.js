(()=>{const $=id=>document.getElementById(id);let secret='',all=[];const money=n=>(Number(n)||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const labels={aceito:'Pedido recebido',em_preparo:'Em preparo',saiu_entrega:'Saiu para entrega',finalizado:'Finalizado',cancelado:'Cancelado'};function card(o){const st=o.status||'aceito',cls=st==='aceito'?'novo':st==='em_preparo'?'preparo':st==='saiu_entrega'?'entrega':'';const items=(o.items||[]).map(x=>`<div class="item"><b>${Number(x.qty)||1}×</b> ${esc(x.name)}${x.variant?` — ${esc(x.variant)}`:''}</div>`).join('');const addr=o.order_mode==='entrega'?`${esc(o.street)}, ${esc(o.house_number)} · ${esc(o.neighborhood)}${o.complement?` · ${esc(o.complement)}`:''}`:'—';return `<article class="card ${cls}" data-search="${esc(`${o.order_number} ${o.customer_name} ${o.phone}`.toLowerCase())}"><div class="top"><div><div class="num">Pedido #${esc(o.order_number)}</div><div class="time">${o.created_at?new Date(o.created_at).toLocaleString('pt-BR'):''}</div></div><b>${esc(labels[st]||st)}</b></div><div class="customer">${esc(o.customer_name)} · ${esc(o.phone)}</div><div class="meta">${o.order_mode==='entrega'?'🛵 Entrega':o.order_mode==='local'?'🍽️ Local':'🛍️ Retirada'} · ${esc(o.payment||'')}</div><div class="meta">${addr}</div><div class="items">${items}</div>${o.notes?`<div class="meta"><b>Obs.:</b> ${esc(o.notes)}</div>`:''}<div class="${o.paid?'pix paid':'pix'}">${o.paid?'✓ Pagamento confirmado':o.payment==='pix'?'PIX — PAGAMENTO A CONFIRMAR':'Pagamento não marcado como confirmado'}</div><div class="total">${money(o.total)}</div><div class="actions"><button data-k="${esc(o._key)}" data-status="em_preparo" class="${st==='em_preparo'?'active':''}">Em preparo</button><button data-k="${esc(o._key)}" data-status="saiu_entrega" class="${st==='saiu_entrega'?'active':''}">Saiu p/ entrega</button><button data-k="${esc(o._key)}" data-status="finalizado" class="${st==='finalizado'?'active':''}">Finalizado</button><button data-k="${esc(o._key)}" data-paid="${!o.paid}">${o.paid?'Pagamento ✓':'Confirmar pagamento'}</button></div></article>`}function render(){const q=$('search').value.trim().toLowerCase();$('orders').innerHTML=all.filter(o=>!q||`${o.order_number} ${o.customer_name} ${o.phone}`.toLowerCase().includes(q)).map(card).join('')||'<div class="empty">Nenhum pedido encontrado.</div>'}async function load(){const r=await fetch('/api/admin-orders',{headers:{'x-admin-secret':secret}});if(r.status===401)throw Error('Senha incorreta.');const d=await r.json();if(!r.ok)throw Error(d.error||'Erro ao carregar pedidos.');all=d.orders||[];render();$('updated').textContent='Atualizado '+new Date().toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}async function patch(body){const r=await fetch('/api/admin-orders',{method:'PATCH',headers:{'content-type':'application/json','x-admin-secret':secret},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)throw Error(d.error||'Não foi possível atualizar.');await load()}$('enter').onclick=async()=>{secret=$('secret').value;try{await load();$('login').classList.add('hidden');$('panel').classList.remove('hidden');sessionStorage.setItem('wippel_admin',secret)}catch(e){$('loginMsg').textContent=e.message}};$('refresh').onclick=()=>load().catch(e=>alert(e.message));$('logout').onclick=()=>{sessionStorage.removeItem('wippel_admin');location.reload()};$('search').oninput=render;$('orders').onclick=e=>{const b=e.target.closest('button');if(!b)return;const body={key:b.dataset.k};if(b.dataset.status)body.status=b.dataset.status;if(b.dataset.paid!==undefined)body.paid=b.dataset.paid==='true';patch(body).catch(x=>alert(x.message))};const saved=sessionStorage.getItem('wippel_admin');if(saved){secret=saved;$('secret').value=saved;$('enter').click()}setInterval(()=>{if(!$('panel').classList.contains('hidden'))load().catch(()=>{})},15000)})();